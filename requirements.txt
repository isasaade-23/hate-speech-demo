# Runtime deps for the Streamlit Community Cloud demo.
pandas>=2.1
numpy>=1.26
# Pinned to the version that produced the model bundle: a newer/older scikit-learn
# unpickles the estimator with an incompatible attribute set (e.g. LogisticRegression
# lost `multi_class`), which breaks predict_proba. Match training = load identically.
# Requires Python >= 3.10 (set 3.11 in the Streamlit app's advanced settings).
scikit-learn==1.9.0
lightgbm>=4.3
lingua-language-detector>=2.0
emoji>=2.10
pyyaml>=6.0
pydantic>=2.5
joblib>=1.3
streamlit>=1.30
